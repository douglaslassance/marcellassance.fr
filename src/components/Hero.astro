---
---

<section id="home" class="hero">
	<div class="container">
		<h1>Marcel Lassance</h1>
		<p class="subtitle">Créateur de Vêtements</p>
	</div>
</section>

<style>
	.hero {
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		text-align: center;
		background: linear-gradient(135deg, #f5f5f5 0%, #ffffff 100%);
	}

	.container {
		padding: 2rem;
	}

	h1 {
		font-size: clamp(3rem, 8vw, 6rem);
		font-weight: 200;
		margin-bottom: 1rem;
		letter-spacing: -0.03em;
	}

	.subtitle {
		font-size: clamp(1.2rem, 3vw, 2rem);
		font-weight: 300;
		color: #666;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}
</style>
